{
  "name": "rs-ecosystem-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "rimraf **/node_modules **/dist **/.turbo"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "turbo": "^1.12.0"
  }
}